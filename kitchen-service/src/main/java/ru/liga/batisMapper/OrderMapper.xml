<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.liga.batisMapper.OrderMapper">
    <resultMap id="order" type="ru.liga.entities.Order">
        <id property="id" column="id"/>
        <result property="customerId" column="customer_id"/>
        <result property="restaurantId" column="restaurant_id"/>
        <result property="status" column="status"/>
        <result property="courierId" column="courier_id"/>
        <result property="timestamp" column="timestamp"/>
    </resultMap>
    <select
            id="getOrders"
            resultMap="order">
            select id, customer_id, restaurant_id, status, courier_id, timestamp
            from orders
    </select>
    <select
            id="getOrderById"
            parameterType="java.lang.Integer"
            resultMap="order">
            select id, customer_id, restaurant_id, status, courier_id, timestamp
            from orders
            where id = #{id}
    </select>
</mapper>